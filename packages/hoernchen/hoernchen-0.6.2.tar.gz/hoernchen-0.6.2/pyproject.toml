[tool.poetry]
name = "hoernchen"
version = "0.6.2"
description = "slqlite3 wrapper based on dataclassses"
authors = ["syntonym <syntaar@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
