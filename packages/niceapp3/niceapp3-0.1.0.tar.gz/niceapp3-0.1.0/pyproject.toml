[project]
name = "niceapp3"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Masuda Shinsuke", email = "shinsuke.masuda@gmail.com" }
]
dependencies = []
readme = "README.md"
requires-python = ">= 3.8"

[project.scripts]
"niceapp3" = "niceapp3:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/niceapp3"]