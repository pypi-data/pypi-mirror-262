# pyproject.toml
[build-system]
requires = ["setuptools >= 30.3.0", "setuptools_scm >= 5"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_scheme = "post-release"
local_scheme = "dirty-tag"
