from setuptools import setup

setup(
    name="bincrypt",
    version="0.2",
    description="A simple way to convert ASCII to binary and the other way",
    author="Matel Matel",
    packages=["bincrypt"],
    zip_safe=False,
)
