# PyCodeSnap - Image Formatter
PyCodeSnap for format codes and code to image.

```python
from pycodesnap import CodeSnap

snap = CodeSnap('python', 'print()')
snap.create(font_size=24)
snap.show()
```