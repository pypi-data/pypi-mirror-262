[project]
name = "model-explorer"
version = "0.0.1"
description = "Model explorer"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
