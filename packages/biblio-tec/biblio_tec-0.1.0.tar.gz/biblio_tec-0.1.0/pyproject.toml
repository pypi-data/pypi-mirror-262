[tool.poetry]
name = "biblio-tec"
version = "0.1.0"
description = ""
authors = ["jonabergamo <jonathanbergamo16@gmail.com>"]
readme = "README.md"
packages = [{include = "biblio_tec"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
hello = "biblio_tec.main:JonaMath.Hello_World"
sum = "biblio_tec.main:JonaMath.sum"
