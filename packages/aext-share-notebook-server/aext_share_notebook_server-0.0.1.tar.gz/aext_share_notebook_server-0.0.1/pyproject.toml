[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aext-share-notebook-server"
dynamic = ["version"]
description = 'The aext-share-notebook-server component of anaconda-toolbox'
readme = "README.md"
license = "BSD-3-Clause"

[tool.hatch.version]
path = "src/aext_share_notebook_server/__init__.py"
