[metadata]
name = pipe2matrix
version = 0.0.1

[options]
packages = pipe2matrix
install_requires = 
	matrix-nio >= 0.23.0
	aiofiles >= 23.2.1

[options.entry_points]
console_scripts = 
	pipe2matrix = pipe2matrix.main:entry_point

[egg_info]
tag_build = 
tag_date = 0

