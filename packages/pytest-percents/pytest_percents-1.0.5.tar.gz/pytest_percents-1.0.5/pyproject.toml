[tool.poetry]
name = "pytest-percents"
version = "1.0.5"
description = ""
authors = ["romanovaleks <romanovaleks@ozon.ru>"]
readme = "README.md"
#packages = [{include = "pytest_percents"}]
classifiers = ["Framework :: Pytest"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.plugins.pytest11]
percents = "pytest_percents.plugin"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
