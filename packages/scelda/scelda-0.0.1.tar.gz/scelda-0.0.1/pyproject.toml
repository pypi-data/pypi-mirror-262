[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "scelda"
version = "0.0.1"
dependencies = [
    "numpy",
    "tqdm",
    "torch",
    "matplotlib",
    "muon",
    "pyro-ppl",
]
