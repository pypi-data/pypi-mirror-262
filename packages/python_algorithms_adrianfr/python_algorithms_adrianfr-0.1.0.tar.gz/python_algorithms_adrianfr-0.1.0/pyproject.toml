[tool.poetry]
name = "python_algorithms_adrianfr"
version = "0.1.0"
description = "A project with simple Python algorithms"
authors = ["Adrian Frączek <adrian.fra97@gmail.com>"]
readme = "README.md"
license = "MIT"
packages = [{include = "algorithms"}]
[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
